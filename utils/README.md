# Utils

Some utilities that might be helpful or just don't fit anywhere else.
- `StopWatch.py` for measuring computation times
- `Roberta.py` a singleton for providing the RoBERTa model without initializing it
everytime it is used somewhere in the code, as this takes a while